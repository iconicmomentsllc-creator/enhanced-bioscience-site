export const metadata = {
  title: "Create account | Enhanced Bioscience",
  description: "Register for member catalog access",
};

export default function RegisterLayout({ children }) {
  return children;
}
