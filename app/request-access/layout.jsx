export const metadata = {
  title: "Request access | Enhanced Bioscience",
  description: "Request access to the private research catalog",
};

export default function RequestAccessLayout({ children }) {
  return children;
}
